# 총무·감사 모바일 화면 목록 및 와이어프레임 정의서

- **작성자**: 이승호 (앱 — 총무·감사 화면 담당)
- **브랜치**: `feat/app-council`
- **작업 경로**: `app/lib/screens/council/`
- **대상 사용자 역할 (Role)**: `TREASURER` (회계/총무), `AUDITOR` (감사), `PRESIDENT` (학생회장)
- **규격 문서**: `docs/enums.md`, `backend/README.md`

---

## 1. 화면 목록 개요

| No | 화면명 | 파일 경로 | 주요 역할 및 기능 | 연동 대상 |
|:---|:---|:---|:---|:---|
| **0** | **총무 메인 대시보드** | `council_home_screen.dart` | 5대 핵심 업무 바로가기, 잔액/예산 현황 요약 | `GET /balance`, `GET /budgets` |
| **1** | **지출 등록** | `expense_create_screen.dart` | 영수증 촬영/첨부, 금액/상호명/예산항목 입력 | `POST /entries` (`EXPENSE`) |
| **2** | **수입 등록** | `income_create_screen.dart` | 학생회비/지원금 입금 내역 및 증빙 등록 | `POST /entries` (`INCOME`) |
| **3** | **승인 대기 목록** | `approval_list_screen.dart` | PENDING 건 검토, 생체인증(`local_auth`) 서명 승인/반려 | `GET /entries`, 컨트랙트 서명 |
| **4** | **장부 정정 신청** | `correction_screen.dart` | 기등록 건 오류 수정 신청 (`CorrectionReason` 4종) | `docs/enums.md` (`correction_reason`) |
| **5** | **학생 이의 답변** | `inquiry_response_screen.dart` | 학생 이의제기 건 열람 및 총무단 소명 답변 작성 | 학생-총무 소통 인터페이스 |

---

## 2. 화면별 와이어프레임 및 상세 기능

### 2.1 [화면 1] 지출 등록 (`expense_create_screen.dart`)
총무가 학생회비를 집행한 후 영수증을 증빙으로 등록하는 화면입니다.

```
┌──────────────────────────────────────────────┐
│  <  지출 등록 (총무)                         │
├──────────────────────────────────────────────┤
│ ┌──────────────────────────────────────────┐ │
│ │          [ 📷 영수증 사진 촬영/첨부 ]      │ │
│ │       (image_picker 연동 / OCR 미리보기)  │ │
│ └──────────────────────────────────────────┘ │
│                                              │
│  지출 항목명 *                               │
│  [ 신입생 환영회 행사 다과 구매          ]   │
│                                              │
│  사용처 (상호명) *                           │
│  [ 청년피자 인하대점                     ]   │
│                                              │
│  지출 금액 (원) *                            │
│  [ 120,000                               ]   │
│                                              │
│  예산 카테고리 *                             │
│  [ 행사비 ▼ ] (행사비 / 사업비 / 운영비)     │
│                                              │
│  지출 일시                                   │
│  [ 2026-09-13 18:30 (날짜 선택)         ]   │
│                                              │
│  상세 메모 (선택)                            │
│  [ 참가 인원 35명 간식 지원              ]   │
│                                              │
│ ┌──────────────────────────────────────────┐ │
│ │  [ 지출 등록 신청 (대기 상태로 저장) ]   │ │
│ └──────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```
- **주요 로직**:
  - `image_picker`로 카메라 촬영 또는 갤러리 선택.
  - 등록 시 `status: PENDING` 상태로 저장되어 예산 잔액에서 즉시 차감되지 않음 (감사/회장 승인 후 반영).

---

### 2.2 [화면 2] 수입 등록 (`income_create_screen.dart`)
학생회비 납부, 학교 교비 지원금 등 입금 내역을 장부에 등록하는 화면입니다.

```
┌──────────────────────────────────────────────┐
│  <  수입 등록 (총무)                         │
├──────────────────────────────────────────────┤
│  수입 항목 / 출처 *                          │
│  [ 2026학년도 2학기 컴퓨터공학과 학생회비    ] │
│                                              │
│  입금 금액 (원) *                            │
│  [ 5,000,000                             ]   │
│                                              │
│  수입 분류                                   │
│  [ 학생회비 납부 ▼ ] (지원금 / 찬조금 / 기타) │
│                                              │
│  입금 일시                                   │
│  [ 2026-09-01 (달력 선택)                ]   │
│                                              │
│  통장 입금 메모 및 확인 정보                 │
│  [ 농협 학생회 전용 통장 입금 내역 확인   ] │
│                                              │
│ ┌──────────────────────────────────────────┐ │
│ │           [ 수입 내역 등록하기 ]         │ │
│ └──────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

---

### 2.3 [화면 3] 승인 대기 목록 (`approval_list_screen.dart`)
감사(`AUDITOR`) 및 학생회장(`PRESIDENT`)이 총무가 올린 `PENDING` 건을 검증하고 최종 집행 승인하는 핵심 화면입니다.

```
┌──────────────────────────────────────────────┐
│  <  승인 대기 목록 (감사/회장)               │
├──────────────────────────────────────────────┤
│  [전체 2건]                  필터: [대기순 ▼]│
│ ┌──────────────────────────────────────────┐ │
│ │ [행사비]  2026-09-10 18:30        PENDING│ │
│ │ 청년피자 개강총회 다과 주문              │ │
│ │ 사용처: 청년피자  |  금액: 120,000원      │ │
│ │ [영수증 보기] [OCR 검증: MATCH]          │ │
│ │ ──────────────────────────────────────── │ │
│ │          [반려]       [승인 (생체 서명)] │ │
│ └──────────────────────────────────────────┘ │
│                                              │
│   * [승인] 클릭 시 생체인증 팝업 발생:       │
│   ┌──────────────────────────────────────┐   │
│   │ 지출 집행 온체인 서명                │   │
│   │  지문 / Face ID 인증이 완료되면      │   │
│   │  스마트 컨트랙트에 영구 기록됩니다.  │   │
│   │             [인증 진행]              │   │
│   └──────────────────────────────────────┘   │
└──────────────────────────────────────────────┘
```
- **주요 로직**:
  - `local_auth` 플러그인 호출하여 생체인증 성공 시 서명 생성 및 `CONFIRMED` 처리.

---

### 2.4 [화면 4] 장부 정정 신청 (`correction_screen.dart`)
이미 등록되었거나 확정된 장부 건에 오기입이 발생했을 때 블록체인의 불변성 원칙 하에 사유를 명시하고 정정(수정 내역을 신규 블록에 기록)하는 화면입니다.

```
┌──────────────────────────────────────────────┐
│  <  장부 정정 신청                          │
├──────────────────────────────────────────────┤
│  정정 대상 내역 ID (Entry ID) *              │
│  [ 2                                     ]   │
│                                              │
│  정정 사유 분류 (`docs/enums.md`) *          │
│  [ 입력 오류 (INPUT_ERROR)              ▼ ] │
│  * 선택 가능: INPUT_ERROR, RECEIPT_RECHECK,  │
│               REFUND, RECLASSIFY             │
│                                              │
│  정정 상세 사유 *                            │
│  [ 실제 영수증 상 금액과 입력 금액 10원 차이│ │
│    발생하여 수정 승인 요청               ]   │
│                                              │
│  [ 📎 수정 증빙 영수증 재첨부 ]             │
│                                              │
│ ┌──────────────────────────────────────────┐ │
│ │             [ 정정 신청 제출 ]           │ │
│ └──────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

---

### 2.5 [화면 5] 학생 이의 답변 (`inquiry_response_screen.dart`)
학생 화면(`screens/student/`)에서 학생들이 지출 내역에 제기한 의문이나 영수증 불일치 신고에 대해 총무단이 공식 소명하는 화면입니다.

```
┌──────────────────────────────────────────────┐
│  <  학생 이의 신청 및 소명                   │
├──────────────────────────────────────────────┤
│  필터: [● 답변 대기 (1)]  [○ 답변 완료 (1)]   │
│ ┌──────────────────────────────────────────┐ │
│ │ [답변 대기]  관련 내역 #1  2026-09-11     │ │
│ │ Q. 신입생 환영회 물품 35,000원 건 세부     │ │
│ │    품목 내역서가 누락되어 있습니다.        │ │
│ │                                          │ │
│ │                       [ 답변 작성하기 ]  │ │
│ └──────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────┐ │
│ │ [답변 완료]  관련 내역 #2  2026-09-12     │ │
│ │ Q. 청년피자 다과 주문 건 참석자 명단?     │ │
│ │ 총무단 답변: 출석 서명부 스캔본 증빙 추가  │ │
│ │                       [ 답변 수정하기 ]  │ │
│ └──────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

---

## 3. 앱 실행 및 시연 매뉴얼 (팀원 공유용)

이 브랜치를 머지하거나 클론받은 팀원이 로컬 환경에서 모바일 앱을 즉시 실행하고 테스트할 수 있는 단계별 가이드입니다.

### 3.1 개발 환경 요구사항 (Prerequisites)

1. **Flutter SDK 설치**:
   - Flutter 3.x 이상 버전 필요
   - 터미널에서 `flutter --version`으로 정상 설치 여부 확인
2. **Android Studio & SDK 설정**:
   - Android Studio 설치 후 `Android SDK Command-line Tools`, `Android SDK Platform-Tools` 설치
3. **가상 기기 (에뮬레이터) 준비**:
   - Android Studio Device Manager에서 Google Play가 지원되는 가상 기기(예: Pixel 7 / API 34 등) 생성 및 실행

---

### 3.2 패키지 설치 및 앱 실행 (Quick Start)

터미널을 열고 아래 명령어를 순서대로 실행합니다:

```bash
# 1. 앱 디렉터리로 이동
cd app

# 2. 필수 의존성 패키지 설치 (image_picker, local_auth, google_fonts 등)
flutter pub get

# 3. 연결된 기기 또는 실행 중인 에뮬레이터 확인
flutter devices

# 4. 에뮬레이터에서 앱 실행 (기기 ID는 flutter devices 출력값 참조)
flutter run -d emulator-5554
```

> 💡 **개발 팁**: 앱 실행 중 터미널에서 `r`을 누르면 핫 리로드(Hot Reload), `R`을 누르면 핫 리스타트(Hot Restart)가 수행되어 코드 수정 사항이 즉시 반영됩니다.

---

### 3.3 역할별 테스트 계정 및 시연 가이드 (Test Accounts)

로그인 화면(`RoleLoginScreen`)에서 실제 ID/PW 입력 폼 및 하단 **'테스트 계정 빠른 선택' 원클릭 칩**을 통해 각 직책으로 접속할 수 있습니다.

| 계정 ID | 비밀번호 | 역할 (Role) | 주요 기능 및 화면 특징 |
|:---:|:---:|:---|:---|
| **`block1`** | `1234` | **총무 / 회계** (`TREASURER`) | • 상단 `총무·회계 모드` 뱃지<br>• 최상단 **`1. 지출 등록` & `2. 수입 등록` 퀵 액션 카드** 강조<br>• 모든 등록/정정/소명 작성 메뉴 활성화 |
| **`block2`** | `1234` | **감사** (`AUDITOR`) | • 상단 `감사 모드` 뱃지<br>• 최상단 **`3. 승인 대기 목록 (생체 서명)` VIP 결재 배너** 강조<br>• 지출/수입 등록 시 `총무 전용 🔒` 권한 안내 토스트 |
| **`block4`** | `1234` | **학생회장** (`PRESIDENT`) | • 상단 `학생회장 모드` 뱃지<br>• 최상단 **`3. 승인 대기 목록` 최종 결재 배너** 강조<br>• 전자서명 승인 및 장부 총괄 권한 |
| **`block3`** | `1234` | **학생** (`STUDENT`) | • 학생 전용 잔액 및 영수증 열람 화면(`StudentHomeScreen`) 분기 |

---

### 3.4 하드웨어 및 권한 검증 테스트 방법 (Hardware Testing)

메인 대시보드 하단의 **`카메라 & 생체인증 테스트`** 배너를 클릭하여 진입합니다:

1. **생체인증 (지문 / Face ID) 테스트**:
   - `[생체인증 팝업 테스트 실행]` 버튼 클릭
   - **Android 에뮬레이터인 경우**: 에뮬레이터 우측 패널의 점 3개(`...`) > **Fingerprint** 메뉴에서 `Send Fingerprint` 버튼을 누르면 가상 지문 터치가 인식되어 인증이 완료됩니다.
2. **카메라 및 갤러리 (영수증 촬영) 테스트**:
   - `[카메라 촬영]` 버튼 클릭: 가상 카메라 뷰파인더가 구동되어 사진 촬영 및 미리보기가 정상 동작합니다.
   - `[갤러리 선택]` 버튼 클릭: 기기 내 저장된 샘플 영수증 이미지를 선택하여 불러올 수 있습니다.

